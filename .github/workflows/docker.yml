name: 🚀 Build & Deploy Microservices

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4  # Mise à jour vers la dernière version

      - name: 📦 Setup JDK
        uses: actions/setup-java@v4  # Mise à jour vers la dernière version
        with:
          java-version: '17'
          distribution: 'corretto'
          cache: maven

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v3  # Mise à jour vers la dernière version
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: ⚙️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3  # Mise à jour vers la dernière version

      # Fonction générique pour build et push des services
      - name: 🏗️ Build and Push Services
        env:
          DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
        run: |
          services=(
            "config-server"
            "discovery"
            "gateway"
            "customer"
            "Product"
            "order"
            "finances"
            "notification"
          )

          for service in "${services[@]}"; do
            # Déterminer le chemin du service
            service_path="./services/${service}"
            
            # Nom du service pour Docker Hub
            service_name=$(echo "${service}" | sed 's/^./\U&/')
            
            echo "🔨 Building ${service} service..."
            cd "${service_path}"
            
            # Build Maven
            ./mvnw clean package -DskipTests
            
            # Build Docker Image
            docker build -t "${DOCKER_HUB_USERNAME}/ecommerce-${service}:latest" .
            
            # Push Docker Image
            docker push "${DOCKER_HUB_USERNAME}/ecommerce-${service}:latest"
            
            cd ../..
          done

      # Préparation du serveur distant avec des vérifications améliorées
      - name: 🛠️ Prepare Remote Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Activer le mode strict pour les erreurs
            set -e

            # Mise à jour des paquets avec gestion des erreurs
            sudo apt update -y || { echo "Erreur lors de la mise à jour des paquets"; exit 1; }

            # Installer les dépendances nécessaires
            sudo apt install -y apt-transport-https ca-certificates curl software-properties-common || \
            { echo "Erreur lors de l'installation des dépendances"; exit 1; }

            # Installation de Docker
            if ! command -v docker &> /dev/null; then
              echo "Installation de Docker..."
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add - || \
              { echo "Erreur lors de l'ajout de la clé GPG Docker"; exit 1; }
              
              sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" || \
              { echo "Erreur lors de l'ajout du dépôt Docker"; exit 1; }
              
              sudo apt update -y
              sudo apt install -y docker-ce docker-ce-cli containerd.io || \
              { echo "Erreur lors de l'installation de Docker"; exit 1; }
              
              sudo systemctl start docker
              sudo systemctl enable docker
              sudo usermod -aG docker $USER
            fi

            # Installation de Docker Compose
            if ! command -v docker-compose &> /dev/null; then
              echo "Installation de Docker Compose..."
              sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose || \
              { echo "Erreur lors du téléchargement de Docker Compose"; exit 1; }
              sudo chmod +x /usr/local/bin/docker-compose
            fi

            # Installer tar si nécessaire
            sudo apt install -y tar

      # Copie sécurisée du docker-compose.yml
      - name: 📂 Copy docker-compose.yml
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: 
            - "./docker-compose.yml"
            - "./.env"  # Ajout optionnel du fichier d'environnement
          target: ${{ secrets.TARGET_DIR }}
          strip_components: 1
          overwrite: true

      # Déploiement avec gestion des erreurs
      - name: 🚀 Deploy Services
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Mode strict et gestion des erreurs
            set -e

            # Créer le répertoire s'il n'existe pas
            mkdir -p ${{ secrets.TARGET_DIR }}
            cd ${{ secrets.TARGET_DIR }}

            # Vérifications préalables
            [ -f docker-compose.yml ] || { echo "Erreur : docker-compose.yml manquant !"; exit 1; }

            # Login Docker Hub sécurisé
            echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin || \
            { echo "Erreur de connexion à Docker Hub"; exit 1; }

            # Pull et déploiement avec gestion des erreurs
            docker-compose pull || { echo "Erreur lors du pull des images"; exit 1; }
            docker-compose up -d || { echo "Erreur lors du déploiement"; exit 1; }

            # Vérifications post-déploiement
            docker-compose ps
            docker-compose logs --tail=50